# Frontend production build — copy to `.env.production` (gitignored) before `npm run build`.
# Use the exact HTTPS Railway URL for the API; path must include `/api` (matches Express mount).

VITE_BASE_URL=https://digisol-ims-backend-production.up.railway.app/api

# Optional: Supabase storage (see src/config/fileUpload.ts)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
